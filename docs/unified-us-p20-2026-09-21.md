# Unified US + P20 integration — 2026-09-21

## Accepted scope and integration plan
The user requested one updated APK containing the prior English/Chinese and custom-package upgrades together with the newest P20 hardware functionality. Preserve source workspaces and parallel backend/creator changes. No production deployment or enabling real payments in this task.

1. Start isolated branch from 3e0d4da; restore App source from the actual 0.1.53+54 release snapshot, then three-way integrate US worktree changes against 83db4a3.
2. Resolve framing/live-playlist conflicts in favor of real device lists/shared session and localize new UI using the US resource catalog. Keep A sound/silent and B video-only processing, 298x298 at20fps, confirmed upload progress.
3. Preserve latest backend operator/content management while merging US custom plans, immutable order price snapshots, audio20%, localization and account/governance endpoints. Keep real payments disabled.
4. Verify Flutter both team compatibility and actual us_free build, locale persistence/system changes, custom plans/private delivery, P20 and latest creator preview. Verify backend with media integration and operator isolation.
5. Build version0.1.54+55 debug candidate with existing development certificate, explicit us_free/API flags. Formal release signing retains mandatory external credentials, with no debug fallback. Commit only code/docs/tests and push unified branch. Deliver source/build manifest and APK hash.

## Interface decisions
- Release App vs US App: three-way merge kept latest creator/media and P20 changes; two conflict files resolved semantically, not by replacing latest source with old localized drafts.
- US native billing implementation is explicitly unavailable; checkout remains disabled. No claim of real Apple/Google billing.
- Current operator authentication takes precedence over older US operator modules; new routes must retain granular operator scopes.
- Current US custom-plan entry in Profile is kept; generic paid videos and creator settlement remain disabled in us_free. Existing team behavior remains available via explicit alternate build configuration.
- Newer creator thumbnail/retry controls were absent from US resource work; regression tests failed before resource fixes and passed after.

## Review focus
Look for accidental loss of latest P20 shared-client/protocol integration, English strings missing on newly reachable screens, permission relaxation from merging two backend auth systems, custom private media/order ownership, real-payment fail-open, stale account response display, release signing bypass, and newest content/creator flow regressions. Compare actual source to both D:/HildorsBuilds/p20-upload-0.1.53-54 and the read-only us-launch worktree. Build/tests are ongoing and must be reported from fresh final results. No live hardware/iOS verification claimed.

## Final verification and artifact
- Default team Flutter suite: 525 passed; us_free Flutter suite: 525 passed. Each default run skipped 2 local backend e2e tests; both were then explicitly enabled and passed (2/2) against this merged backend.
- Complete Flutter static analysis: No issues found.
- Backend Node24/real FFmpeg integration: 225 passed, 0 failed or skipped.
- Fresh independent integration review found no Critical/Important integration faults. Its player retry translation omission was treated as required bilingual scope: failing English stalled-player test reproduced it; localized retry passed all14 player tests and both full suites.
- Localized resource source fragments include the new P20 strings; regenerating resources must not erase them.
- APK: releases/hildors-unified-0.1.54-55-debug.apk, 321002508 bytes, SHA256 C701F7F448BB2A0B63B4A3F8DE9A1D2D2BC8E09AAF3B522574485078364E9101.
- applicationId com.hildors.hildors_cockpit; versionName0.1.54/versionCode55; development certificate matches0.1.53. This is a debug test APK, not an App Store/Play submission artifact. No private signing keys are committed.
- Build: flutter build apk --debug --no-pub --dart-define=HILDORS_RELEASE_PROFILE=us_free --dart-define=HILDORS_API_BASE_URL=https://api.hildors.com. Reproducible wrapper: tool/release-us-android.ps1 -Mode DebugApk -BuildName 0.1.54 -BuildNumber 55.

## User verification and remaining dependencies
Language setting: Profile → Device settings → Language; system mode uses Chinese variants→Simplified Chinese and otherwise English; manual choice survives restart. Custom packages/orders: Profile → Custom video packages. No generic paid content, membership, recharge or creator settlement is enabled by us_free.

Native Apple/Google billing and real store price synchronization remain unavailable; the merge preserves tested disabled adapters, not live checkout. Public production API read-only probes confirmed expected plans/audio20% and purchaseEnabled:false; authenticated production order/deletion/governance flows were not mutated or fully verified here. The backend source is integrated but not deployed by this task.

English UI is independent of catalog editorial translation. Production sample lacked English content and correctly returned original Chinese; operations must author/review English titles/stories before US launch. Account deletion remains a reviewed request, not an automatic purge; unresolved retention terms are not silently invented.

No phone/P20 hardware or iOS/macOS build was available in this run. Verify actual A/B list, sound/silent upload, progress, playback and language change on phone. Debug timing is not a production performance benchmark.

## Source of truth and rollback
Continue unified work in D:/HildorsBuilds/hildors-unified-0.1.54-55, branch codex/unified-us-p20-20260921. Source workspaces and other tasks remain untouched; do not build the old shared checkout and label it unified. Use this committed branch plus explicit us_free build flags.
No production deployment/migration occurred. Before future deployment, compare current server source, preserve manual code/database backup, stage with temporary data and validate auth/media/custom order flows; do not enable automatic backups. Roll back new entry points/code without deleting additive tables or new orders. Android rollback should rebuild previous code with a higher version code rather than uninstalling and losing local files.
