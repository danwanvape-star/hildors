# 受控视频交付接口（本地验证阶段）

后端提供按包内单视频获取清单与文件的基础接口：

- `GET /v1/me/packages/{packageId}/clips/{clipId}/manifest`
- `GET /v1/me/packages/{packageId}/clips/{clipId}/download`

两个接口均要求用户Bearer会话；清单中的相对地址不是公开链接，也不包含令牌。每次请求重新检查会话、有效权益、已发布状态、审核及媒体检查结果。下架/撤销阻止后续请求，不能收回已经下载的文件或已开始的传输。

清单包含字节数、SHA-256、包版本及视频ID，供后续App校验下载缓存。文件支持单段Range断点读取。文件大小变化会阻止交付；App仍须完成SHA-256校验才能提交本地文件。暂不提供CDN签名链接。

默认关闭，仅测试或可信本地集成代码显式设置`app(store, { enableDownloads: true })`启用。CLI及App仍未开放下载，bootstrap能力维持关闭，不改变现有Demo体验。不存在面向用户的注册、登录或自动领取权限接口。

只交付经过审核的普通目录视频；不接入定制订单原始素材。此MP4不是厂家转码文件，`hardwareReady`始终为false。内置Demo没有后台媒体文件，不能用此接口下载。

测试使用合成字节验证交付和权限，不作为真实视频转码或手机播放验收。后续需要App安全保存会话、下载临时文件与哈希校验、取消/重试，再做真实素材端到端验收。

## App缓存模块增量

已增加`VerifiedVideoDownload`（Android/iOS/桌面IO模块，未连接页面）：读取受认证清单、限制同源固定下载路径、不跟随重定向、流式写入唯一临时目录、校验字节数与SHA-256后重命名为MP4。错误时清理本次临时目录，不覆盖既有文件。会话只作为调用参数，不写入磁盘。只允许HTTPS或本机HTTP联调。

单文件暂限256MB，与当前本地后台一致，不代表硬件限制。当前失败后重新下载，不支持客户端断点恢复、取消按钮、缓存索引或启动时清理崩溃残留。缓存根目录由后续集成层提供App私有路径，不写手机相册。

本轮本机HTTP测试通过：正常保存、相同长度的损坏文件拒绝、外部清单地址拒绝、401拒绝以及失败清理。仍未做真实手机登录下载联调。

## 下载状态增量

增加单视频`VideoDownloadController`：下载中、校验中、取消中、已取消、失败、完成状态，以及字节进度和用户可读错误。取消会关闭本次网络客户端，清理结束后才允许重试；校验及提交前后也检查取消。控制器销毁时取消任务，不再向页面发送通知。每次重试需重新传入会话，不在控制器中长期保存令牌。重试从头开始，不是断点续传。

本机HTTP测试增加了校验阶段取消与清理、失败后重试成功、完成后重复调用不重下、预先取消拒绝。页面按钮、安全会话获取、崩溃残留清理及真机断网测试仍待集成；没有更新APK。

## 详情页下载面板

增加`VideoDownloadPanel`，展示进度、校验、取消清理、失败重试及“已保存到App本地 · 未发送设备”。默认禁用，无下载按钮；启用后由注入的会话获取方法确认登录，不在页面里硬编码令牌。父级负责持有并销毁下载控制器。

`RemoteCatalogPage.clipActions`提供包内每条视频的操作区插槽，供认证集成层挂载面板。默认调用方尚未配置此插槽，保持只读；并非当前安装包已经开放下载。通用目录页不直接导入IO下载实现，保留Web编译边界。实际登录、安全会话存储、后台能力开关读取与控制器生命周期装配仍需完成。

新增页面测试覆盖默认禁用与缺少会话时阻止下载；目录和下载缓存回归测试一并执行。

## 权限检查与路由装配

增加`GET /v1/me/packages/{packageId}/clips/{clipId}/access`，使用同一后台权益判定。返回允许、未启用或内容不可用；会话过期返回401。Bootstrap的cloudDownload现在反映服务实例开关，CLI默认仍为false。权限检查不保证文件存在或完整，最终manifest/download仍检查文件。

增加IO组件`RemoteDownloadActions`，可通过详情页clipActions插槽装配：父级提供下载服务、包/视频ID及获取当前会话的方法。组件自动检查权限、提示重试，允许后挂载下载面板；点击下载再次检查权限。组件持有控制器，离开路由时销毁并取消任务。下载文件依然需要由后续缓存索引管理。

真实登录及安全会话存储尚未提供，因此默认内容库调用方没有启用这个组件。没有编译进固定令牌，也未开放匿名下载或更新APK。新增测试覆盖权限响应与未登录页面，后端覆盖权益撤销和开关。

## 下载记录持久化

下载完成后在该任务目录写入record.json（先写临时记录，再重命名），包括服务来源、包/视频ID、字节数和SHA-256，不保存令牌或任意外部路径。恢复时固定读取同目录video.mp4，跳过链接、无效记录、半成品和哈希不符文件，不自动删除它们。

控制器提供restore，详情页权限通过后调用它。重新创建控制器仍能识别已验证下载；恢复期间若启动了新下载，旧恢复结果不会覆盖新任务。调用方必须传入App私有且按账号隔离的缓存目录；账号登录集成尚未提供，不应使用跨账号公共目录。此记录只代表本地完整文件，不代表当前云端权益或硬件就绪，也不负责判断新版内容是否需要更新。

测试覆盖新控制器恢复、不同服务来源/视频ID隔离、同长度损坏文件拒绝。默认App仍未启用远程下载，尚未更新APK或完成真机重启验收。
