# 后台视频处理验收

日期：2026-09-14。范围：本地Node/SQLite原型，不包含云部署、App接入或硬件转码。

## 已完成

- FFmpeg/ffprobe 9.0.1本机可执行，7z包SHA256已验证。
- 四个现有Demo走通真实上传、完整解码、480×480缩略图、隔离测试审核和公开目录元数据查询。
- 缩略图采用等比缩放加黑边，不新增画面裁切；抽查02与04图片显示正常。
- 公开目录不包含内部审核记录、授权材料引用或私有素材信息。
- 再次检查立即撤销旧审核；处理期间不能通过旧审批发布；替换素材后旧处理结果不能写入。
- 全套9个测试通过，0失败，0跳过。浏览器创建/列表验收沿用前一轮结果。

## 真实素材参数

| 视频 | 分辨率 | 时长（秒） | 视频/音频编码 | 检查结果 |
|---|---|---|---|---|
| showcase_01.mp4 | 720×720 | 62.508 | H.264 / AAC | 通过 |
| showcase_02.mp4 | 720×1264 | 10.033 | H.264 / AAC | 通过 |
| showcase_03.mp4 | 720×406 | 10.054 | H.264 / AAC | 通过 |
| showcase_04.mp4 | 720×1280 | 10.472 | H.264 / AAC | 通过 |

## 重复验收

在项目根目录运行：

```powershell
node backend/check-tools.mjs
$env:HILDORS_MEDIA_INTEGRATION = '1'
node --test backend/test/*.test.mjs
```

生成记录：`backend/data/acceptance/pipeline.json`及四张缩略图（本机生成文件，不提交仓库）。

## 边界

解码检查通过仅证明本机工具能够处理该媒体，不代表所有手机兼容、不代表版权审核真实完成，也不代表P20/P11可播放。硬件转码、受控下载、用户权益和云端发布仍待接入。测试审核使用明确的TEST-ONLY记录，限独立内存数据库。
