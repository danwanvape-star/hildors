# 定制入口恢复 0.1.57+58

美国首发配置的发现页允许展示后台visible的customization板块，指向现有CustomPlansPage，不再进入旧的创作者报价原型。使用现有中英文资源显示套餐标题及支付暂未开放提示。我的页原有定制入口保留，通用付费视频及真实商店支付未开启。

本次仅修改ExplorePage、入口回归测试、版本号；无后端或数据迁移。后台仍可通过页面装修隐藏卡片。用户可查看套餐、已有订单并进入需求审核流程；实际服务端可用性和账号登录仍由现有接口决定。

验证：美国首发配置Flutter完整测试544通过、2跳过；静态检查通过。入口跳转、支付提示及套餐有声/无声提交的定向4项测试通过。未进行真机安装或真实支付测试。

交付采用ARM64调试签名包，保留原签名。构建参数包含--split-per-abi --target-platform android-arm64 -P force-version-code-ignoring-abi=true，避免ABI自动加版本号。正式发布仍需公司正式签名。

回退：撤销本次独立提交并以更高版本号构建旧代码，避免卸载导致本地数据丢失；无服务器部署或数据库变更。
