# P20 手机通信诊断版 0.1.58+59

用户真机反馈：列表不可读，音频上传在awaitingReady超时，确认字节0/20848。该证据只能确认没有收到有效的上传许可，不能区分设备未应答与解码丢弃。电脑目前不在设备局域网，用户只能手机测试。

本轮不改变CRC、帧长度、命令语义或超时长度；增加仅含元数据的诊断：超时命令、当前TCP连接累计接收字节、有效帧、完整帧CRC/尾错误计数。上传失败页面可选择复制此文本；不记录文件名、音频、视频、密码或原始应答载荷。

rx=0：此连接尚未收到任何字节；rx>0且valid=0：收到数据但没有完整有效帧（可能头/长度/CRC/尾不匹配或数据不完整）。rejected只统计边界已确定的CRC/尾错误，不代表所有丢弃数据。valid>0说明该连接曾解码过有效帧，不保证当前命令有应答。计数是连接累计，非单命令。

本轮未确认实机根因，不能称为修复完成。需要用户完全退出厂家App后重连Hildors，只尝试一次上传并提供包含P20 cmd诊断行的截图，再据实机证据继续处理。不会因拿不到应答盲目切换协议或放宽CRC。

新增真实loopback Socket测试分别验证无回包和坏CRC分类；既有上传页面测试验证诊断显示。交付保留统一版双语、定制入口及硬件转码功能，仅ARM64调试包。无后端部署或数据修改。

验证：完整测试546通过、2跳过；静态检查通过；ARM64 APK构建成功，版本0.1.58+59。仍需真机诊断结果，不宣称已修复列表及上传。

## 0.1.59+60：跟进真实手机结果

用户截图：0x31超时，rx=21 valid=1 rejected=0，awaitingReady。可确认连接曾收到数据并解析过有效帧，但不能确认该帧属于0x31，亦不能排除残留不完整帧。上一版累计统计不够定位根因。

补充last（最后解码成功命令）、deltaRx（发送当前请求前至超时累计新增字节）、pending（残留接收字节）、len（残留头声明长度，头不足5字节时-1）。不记录文件名、媒体、原始载荷；不修改CRC或协议兼容方式。

新增模拟：先收到0F的16字节有效帧，再收到下一应答的5字节帧头，总计21字节、1有效帧。验证能区分last=0x0f、deltaRx=5、pending=5、len=2。这只是证明同一累计数字存在多种解释，并非声称实机正在返回此序列。

31项通信/上传定向测试通过；静态检查通过。还需实机包含新字段的截图，未确认根因、未宣称列表和上传已修复。本轮继续提供ARM64调试包。

## 0.1.60+61：新机器接收许可之后的视频传输停滞

用户确认已换新机器，截图显示视频阶段0x31已返回9字节许可，传输阶段无进度应答，文件15862588字节。旧机器单列表结论不能用于本次故障。未确认实际视频发送量，不能以ACK=0断言手机未发送，也不能据此修改协议为停等或延长超时。

新增失败页TX queued/flushed/ACK：queued为准备交给socket写入的字节（含正在等待flush的块），flushed为本地socket.flush已返回的字节，ACK为设备进度确认。flush并不证明设备已收到或写入SD卡。每块等待写入前后都更新快照，防止卡在flush时页面仍显示旧计数。百分比仍只取设备确认。

22项定向通信/诊断/页面测试通过，静态检查通过。本轮只补齐证据，无协议、发送节奏或超时变更；仍待手机截图定位实机根因。保留中英文、定制、单架构小包；无后台部署。
