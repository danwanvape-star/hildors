# Manufacturer status-based acknowledgements — 0.1.66+67

Manufacturer reviewed firmware and confirmed returned SEQ is directly 2; instructed sending the next block on a progress reply and using status 02 for completion. Real socket logs independently showed first SEQ=2 for A/B uploads.

Upload now accepts structurally valid status 01 regardless of SEQ value, acknowledging only the single outstanding block's actual bytes. SEQ remains visible in raw diagnostics and does not multiply progress. Stop-and-wait, exclusive command queue, frame CRC/length checks, rejection statuses, timeout and completion after all local data is sent remain. Status 02 supports both documented five-byte data and previous short compatibility.

Validation: old implementation failed delayed-ACK fixture with constant SEQ=2; corrected implementation passed 37 protocol, transport, media-flow and wire-log tests. Existing incremental sequences are still tested. Hardware end-to-end result remains unverified. Existing compact playlist and copy-log features retained.

No server deployment or data migration. Rollback: revert this change and rebuild with a higher Android build number. Private branch codex/unified-us-p20-20260921.
