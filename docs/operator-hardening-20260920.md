# 运营账号兼容加固 · 2026-09-20

已部署到现有 Hildors 后台。本分支以现网后台源码为基线，保存依赖的内容管理、创作者、定制套餐、账号删除等现网功能；没有覆盖共享工作区。

本次新增：
- 原有 operators 表新增 must_change_password，旧账号默认无需强制改密；新建和管理员重置后须本人改密。
- 普通运营人员可修改本人密码，不需要运营账号管理权限；改密后所有旧会话失效。
- 保留全部细粒度权限及预设、现有主管理员与服务令牌。
- 账号与来源两级限流，限制桶及会话数量；仅显式信任的本地反向代理可提供经过验证的客户端 IP。
- 账号变更和审计写入使用同一事务；改密退出会使旧页面请求失效并清理业务界面。

验证：210 项后台测试全部通过，0 失败、0 跳过（HILDORS_MEDIA_INTEGRATION=1，Node 24）。包括真实 MP4 上传、解码、预览、权限撤销、迁移兼容和首次改密 HTTP 流程。浏览器已核对首次改密弹窗、关闭后无业务入口以及可再次进入本人改密。没有创建线上测试账号，也未更改任何线上账号密码或权限。

部署：6 个代码文件；数据库副本迁移验证所有原字段值、密码哈希和授权不变。/ready、/admin/me、/admin/operators、/admin/audit 均正常。部署会重启服务，浏览器需重新登录；无需更新 APK。

手动备份：/var/backups/hildors/operator-hardening-20260920T033852Z。没有启用自动备份。

回退：停止 hildors-team-staging.service，将备份 code.tgz 中原代码恢复到 /opt/hildors/backend，再将 operators-rollback-compatible.mjs 覆盖 src/operators.mjs，最后启动服务并验证 /ready。兼容文件仅将旧版账号 INSERT 改成显式列名，使旧代码能使用新增列后的数据库。不要直接恢复旧数据库，以免覆盖上线后新增业务数据；保留新增列和所有已发生的密码/权限修改。新增 operator-throttle.mjs 在旧代码中不会被引用，可保留。回退会撤销强制改密和新增限流功能。

审查修复：可信代理来源识别、改密退出时旧响应竞态，均已添加回归测试。
