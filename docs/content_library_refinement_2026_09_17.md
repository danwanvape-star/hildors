# 内容库与播放列表调整（2026-09-17）

## 已确认的产品行为

- 单条视频和角色视频包在卡片内显示出处：HILDORS 出品、具名创作者或匿名创作者。保留出处筛选，不用来源分组标题替代内容署名。
- 角色包详情使用主图/简介双栏，创作者位于简介下方；小屏与大字号纵向排布以避免挤压。视频以两列缩略图展示，选择后才创建播放器及该视频操作入口。
- 具名创作者可进入作品空间；按稳定 creator.id 汇总已加载的已发布目录，不按名字合并，不包含匿名作品。匿名资料不公开身份。
- 圆形取景继续提供缩放、拖动、完整展示、铺满和保存。修复云端 URL 被误判为本地丢失文件的问题，增强播放列表的明确入口。取景不代表已转码或已上传设备。

## 数据与发布边界

后台目录新增 description 及 creator {id,name,anonymous} 可选资料；旧数据兼容匿名/未补充状态，不虚构简介或姓名。管理员负责填写真实公开署名及稳定公开标识，不能把邮箱、证件或私有账号资料当公开 ID。App 根据公开字段展示；新增后端代码必须部署且内容资料补齐后，线上内容才会显示对应简介和具名创作者。

本次不实现厂家转码协议，不改变用户素材或线上数据库。APK 为现有测试签名，版本 0.1.34+35；不是应用商店正式发布包。自动备份按用户决定暂缓，列为上架前待办。

## 验证记录

后台 21 项测试（含真实媒体检查）通过，隔离浏览器验证已发布内容的资料编辑、匿名默认值、重置和新建保存通过。App 全量 321 项测试通过；静态检查无问题。最后一处测试格式修正后，播放器 4 项专项测试再次通过。播放器在新页面或弹层遮挡时暂停，加载期间被遮挡也不会自动播放。布局截图使用测试角色信息与缺图占位，只用于手机尺寸排版检查。

用户已明确本轮只生成 APK，后台稍后部署。后台更新包基于提交 2d0d5c1，仅包含 server.mjs、store.mjs、app.js、style.css 四个代码文件，保存在 D 盘 releases 目录，尚未上传或部署。已只读核对线上四文件与修改前 main 一致。


Release APK 构建成功，apksigner 校验通过，包版本 0.1.34+35。产物：D:/HildorsTools/releases/hildors-0.1.34+35-content-refinement.apk。SHA256：7BB1F60ADEBACCD33EFADD9E9DBFBC75E05AECBE54E3E9A78D424FB4D0DA5590。未进行 Android 手机或风扇真机验收。
